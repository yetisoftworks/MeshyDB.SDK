{
  "Logging": {
    "IncludeScopes": false,
    "Debug": {
      "LogLevel": {
        "Default": "Warning"
      }
    },
    "Console": {
      "LogLevel": {
        "Default": "Warning"
      }
    }
  },
  "MeshyDB": {
    "ClientKey": "{replace with Client Key from Account}",
    "PublicKey": "{replace with Public Key from Account}",
    "PrivateKey": "{replace with Private Key from Account}"
  }
}
